# Phase 7 — Committed Product Spec

> Template for `07-spec.md`. Fill in only AFTER all three litmus tests pass. The spec is a commitment, not a wishlist — if any field feels speculative, route back to Phase 5 instead of writing it.

## Identity

- **Name**: `<chosen name from Phase 6>`
- **Tagline (≤10 words)**: `<one-liner>`
- **Category**: `<extension / adjacent / universal / new domain>`
- **Ecosystem**: `<Claude Code / Cursor / npm CLI / etc.>`

## Why this product (locked from Phase 1)

- **Brand goal it advances**: `<concrete outcome>`
- **Success criteria (with deadline)**: `<number + date>`
- **Hard constraints respected**: `<list — time/week, money, compliance, energy>`

## Audience

- **Primary user**: `<short persona>`
- **Where they hang out**: `<channels / communities>`
- **What pain it relieves**: `<one or two sentences>`

## v0.1 scope

What ships:
- `<feature 1>`
- `<feature 2>`
- `<feature 3>`

What is explicitly cut from v0.1:
- `<cut 1>` — *deferred / not doing*
- `<cut 2>` — *deferred / not doing*

The cut list matters as much as the included list. Be honest about what's tempting but not in scope.

## Litmus test record

Confirm each test passed. Quote the user's own words where helpful — these become the launch story raw material.

- **Personal use test**: would the user use this 5+ times in next 6 months? **Pass / Fail** — `<why>`
- **Demo feasibility**: can be demoed publicly without compliance violations? **Pass / Fail** — `<why>`
- **30-second pitch**: pitch is sharp, no "well, you see..." backstory? **Pass / Fail** — `<the actual pitch the user wrote>`

## 30-second pitch (final)

The pitch the user wrote during the third litmus test. Lock it here so the launch posts and README inherit the same shape.

> "<verbatim pitch>"

## Kill criteria

Specific conditions under which the user abandons. Each criterion is a binary trigger, not a soft signal. Pre-committing the criteria removes sunk-cost bias later.

- `<e.g. no momentum after 2 weeks of launch posts (under N stars / under M downloads)>`
- `<e.g. platform ships native equivalent before v0.1 reaches feature parity>`
- `<e.g. build estimate exceeds 2x and no path to scope reduction>`

If any criterion fires, re-enter the workflow at Phase 3 (rebrainstorm with the same Phase 1 constraints intact).

## First steps

The minimum work needed to start. Keep it boring — repo init, README scaffold, first issue. The point is to make starting frictionless, not to design the architecture here.

- `<step 1>`
- `<step 2>`
- `<step 3>`

## Open questions

Anything still unresolved that would block work but doesn't kill the spec. Surface them so they don't ambush the build phase.

- `<question 1>`
- `<question 2>`

## Cross-references

- Why & constraints: `01-why-constraints.md`
- Asset inventory: `02-asset-inventory.md`
- Option list: `03-options.md`
- Scan results: `04-niche-scan.md`
- Scoring: `05-scoring.md`
- Naming: `06-naming.md`
